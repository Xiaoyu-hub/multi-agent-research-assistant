# Multi-Agent Research Assistant Dependencies

# Core
fastapi>=0.100.0
uvicorn[standard]>=0.25.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# HTTP & Networking
httpx>=0.25.0
requests>=2.31.0

# Configuration
python-dotenv>=1.0.0
pyyaml>=6.0.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
pytest-xdist>=3.0.0
httpx-sse>=0.4.0

# Vector Store - Chroma (默认)
chromadb>=0.4.0
# 或者使用 Qdrant (可选)
# qdrant-client>=1.7.0

# BM25 for hybrid search
rank-bm25>=0.2.0

# Embedding
sentence-transformers>=2.0.0
huggingface-hub>=0.20.0
# 或者使用 OpenCode Zen embedding
# 已在 opencode-zen 中包含

# Redis for session memory (可选)
redis>=5.0.0

# File handling
python-multipart>=0.0.6

# Logging
loguru>=0.7.0

# OpenCode Zen (LLM Provider)
openai>=1.0.0
aiohttp>=3.9.0

# MCP support (可选)
# mcp>=1.0.0